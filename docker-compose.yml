services:
  api:
    build: .
    container_name: seatly-api
    restart: unless-stopped
    env_file: .env
    ports:
      - '3000:3000'
    networks:
      - app-network

networks:
  app-network:
    external: true
